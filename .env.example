# =======================
# Supabase Configuration
# =======================
# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=<SUBSTITUTE_SUPABASE_URL>
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<SUBSTITUTE_SUPABASE_PUBLISHABLE_KEY>

# =======================
# Cloudflare R2 Storage
# =======================
# For storing STL files (up to 200MB each)
# Get credentials from: Cloudflare Dashboard → R2 → Manage R2 API Tokens
# IMPORTANT: Keep access keys secret, never commit to git
CLOUDFLARE_R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
CLOUDFLARE_R2_ACCESS_KEY_ID=<YOUR_R2_ACCESS_KEY_ID>
CLOUDFLARE_R2_SECRET_ACCESS_KEY=<YOUR_R2_SECRET_ACCESS_KEY>
CLOUDFLARE_R2_BUCKET_NAME=<YOUR_BUCKET_NAME>
# Public URL for file downloads (must have CORS configured)
CLOUDFLARE_R2_PUBLIC_URL=https://<YOUR_PUBLIC_DOMAIN>

# =======================
# Mailjet Email Service
# =======================
# For sending order confirmations and auth emails
# Get credentials from: Mailjet Dashboard → Account Settings → API Keys
# IMPORTANT: Sender domain must be verified (SPF, DKIM, DMARC)
MAILJET_API_KEY=<YOUR_MAILJET_PUBLIC_API_KEY>
MAILJET_SECRET_KEY=<YOUR_MAILJET_SECRET_KEY>
# Verified sender email (must match verified domain in Mailjet)
MAILJET_FROM_EMAIL=noreply@yourdomain.com
MAILJET_FROM_NAME=Additive3D
# Comma-separated emails to receive order notifications
MAILJET_MANAGER_EMAILS=manager@yourdomain.com,manager2@yourdomain.com